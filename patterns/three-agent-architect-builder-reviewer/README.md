# Pattern: Three-Agent Architect–Builder–Reviewer

| Field | Value |
|---|---|
| **Pattern name** | `three-agent-architect-builder-reviewer` |
| **Status** | `trialed` — signed off as the team standard 2026-07-17; promotes to `adopted` after its first real-ticket run (CLAUDE.md promotion rule) |
| **As-of date** | 2026-07-29 (Reviewer moved to Claude Sonnet 5 @ `xhigh` for cost; §7) |
| **Expiry trigger** | First successor release to any listed model, or 2027-01-29 (+6 months), whichever comes first |
| **Sign-off** | Horace Hou (repo maintainer), 2026-07-17 |

One ticket flows through three agents in sequence: **Architect** plans → **Builder** implements → **Reviewer** (fresh context, different model tier) clears or bounces. No agent judges its own work.

## 1. When to use / when not to use

**Use when:**
- Ticket-scoped production changes where a bad merge is expensive: shared services, security-sensitive paths, concurrency-heavy code.
- The ticket is large enough to amortize three agent runs (rule of thumb: ≥ half a day of human-equivalent work).
- The team priority is predictable, reviewable delivery over token cost and latency — an explicit policy of this pattern.

**Do not use when:**
- Trivial or mechanical changes (typo fixes, config bumps, codemod output) — pipeline overhead dominates the value.
- Throwaway spikes and prototypes — a plan plus an independent review of disposable code adds nothing.
- Live pair-programming where a human already reviews every step in real time.

*(Scope guidance is design-derived, not a benchmark claim.)*

## 2. Agent roles & boundaries

| Agent | Does | Never does |
|---|---|---|
| **Architect** (Planner) | Reads the ticket and the codebase; produces the implementation plan. Exploration/tool-call heavy. | Writes production code. |
| **Builder** (Coder) | Implements against the Architect's plan; runs tests; iterates until passing. | Acts as the final judge of its own work; merges without Reviewer clearance. |
| **Reviewer** | Runs in a **fresh context** (never the Builder's session); reviews the Builder's output against the ticket and the plan, focusing on edge cases, concurrency, and security-sensitive paths. Clears the work or bounces it back to the Builder with findings. | Continues the Builder's session; fixes code itself. |

| **Triage** (nightly sweep only) | Classifies open tracker issues — fixable / invalid / needs-human — and synthesizes a cold-startable ticket file from each fixable one. | Fixes code; writes to the tracker (the report step owns all tracker writes). |

The Reviewer is **deliberately a different model tier from the Builder** so the two do not share blind spots. This is a hard requirement of the pattern, not a cost knob.

Read that requirement precisely: it asks for a **different** model, not a **stronger** one. The mechanism is independence — two models fail differently, so one catches what the other misses — and it is satisfied in either direction. Since 2026-07-29 the Reviewer runs a *lower* tier than the Builder (Sonnet 5 judging Opus 5, §3), which keeps the independence the requirement is about while spending capability at the gate. What would break the requirement is collapsing the two onto the same model, in either direction (§4, tier collapse).

**Testing policy `[team-policy]` (2026-07-17):** the agents own the whole test pyramid. The Builder writes and runs unit + integration tests (and E2E tests where the ticket's acceptance calls for them); the Reviewer independently re-runs the full suite; the deliver step re-runs it on the merged default branch. Tests run manually or wired into the pipeline as fits the project. The human tests exactly once — the Gate 2 smoke test after the PRD's tasks are all done.

Related in-house variant: fx-eye-tracking's `planner` / `generator` / `evaluator` harness — same independence principle, heavier machinery (spec decomposition, file-scope parallelism, sprint contracts, tracker governance). That harness runs a whole project; this pattern runs one ticket.

## 3. Model + effort assignment (as of 2026-07-28)

| Role | Model | Effort | Reasoning | Source labels |
|---|---|---|---|---|
| Architect | Claude Opus 5 | `max` | Re-pinned to the Opus tier 2026-07-26, ending the 2026-07-22 Fable 5 trial (§7, issue #48). The trial is closed by the pattern's **successor-release expiry trigger**, not by a result: it ran four days and **no bounce-rate or drift evidence was recorded either way**. The original rationale still holds — the plan is the cold-start contract, and the Reviewer checks the implementation *against* the plan rather than judging whether the plan was right, so an internally-consistent but under-specified plan passes review silently and planning strength has value review cannot backstop — which is why `max` effort stays at this stage. Consequence: Architect and Builder now share a model, and the Reviewer is a different model from **both** (stronger on the §2 independence requirement than the trial configuration, which had Architect and Reviewer sharing one). | `[team-policy]` — maintainer decision, 2026-07-26. Opus 5 as the Opus-tier model "for complex agentic coding and enterprise work": `[official]` — [models overview](https://platform.claude.com/docs/en/about-claude/models/overview), verified 2026-07-26. **No planning-specific benchmark basis claimed** in either direction, for Opus 5 or for Fable 5 |
| Builder | Claude Opus 5 | `xhigh` | Claude Opus 5 is Anthropic's documented default "for complex agentic coding and enterprise work," at Opus 4.8's price ($5/$25 per MTok, 1M context, 128k output). **Effort guidance changed with Opus 5 and is not the 4.8 wording:** Opus 5 starts at `high` by default, with `xhigh` documented as the step-up "for demanding coding and agentic work" — which is the shape of every ticket this pattern targets (§1), so `xhigh` is a deliberate step-up, not the vendor's starting point. The prior pin's SWE-bench Pro figure (Opus 4.8 **69.2** vs Sonnet 5 **63.2**) is **dropped, not carried over** — no verified Opus 5 number is on record. One effort setting for all tickets — no per-ticket downgrades; team priority is predictable delivery over token cost. Open item (§7): Anthropic explicitly advises a fresh effort sweep rather than reusing a prior model's setting; that sweep has not been run. | Model + effort semantics: `[official]` — [models overview](https://platform.claude.com/docs/en/about-claude/models/overview) and [effort](https://platform.claude.com/docs/en/build-with-claude/effort), both verified 2026-07-26. Opus-5-specific benchmark: **none claimed**. `xhigh`-for-all-tickets and the no-downgrade rule: `[team-policy]` |
| Reviewer | Claude Sonnet 5 | `xhigh` | **Cost-driven tier drop from Fable 5, 2026-07-29** (issue #111). Fable 5 is $10/$50 per MTok — the most expensive model in the table, 2× Opus 5 and 3.3× Sonnet 5 ($3/$15; $2/$10 introductory through 2026-08-31, so ~5× today, 3.3× from September). The maintainer's position is that Fable 5 is unaffordable at this repo's and its adopters' run volume, and that a Sonnet 5 reviewer performed acceptably on other projects. Be precise about what this spends: **capability at the gate, not independence.** §2's hard requirement still holds — Sonnet 5 differs from the Builder (Opus 5), so this is *not* the §4 tier-collapse mode, and it is not the Opus 4.8 proposal rejected on 2026-07-22 (that was the Builder's own tier). But it is the **first configuration in which the judge runs a lower tier than the implementer**: Anthropic positions Sonnet 5 as "the best combination of speed and intelligence", which is not the capability-max claim Fable 5 carries, and the pattern's own §2 rationale for a different tier is blind-spot independence, never reviewer superiority — read it that way and this stays coherent; read it as "the gate is the strongest model" and it does not. **Effort `high` → `xhigh`** buys back some depth where the tier dropped, and reverts the 2026-07-28 cut, which existed only to make Fable 5 affordable. `xhigh` is a deliberate step-up: Sonnet 5, like Opus 5, defaults to `high` in Claude Code. Secondary effect, **not measured**: Sonnet 5's comparative latency is "Fast" against Fable 5's "Slower", and the Reviewer sits on every ticket's critical path where §4 records that a parallel wave is paced by its slowest lane — expect shorter runs, do not quote a number. Two alternatives were considered and rejected by the maintainer (#111): risk-tiered review (Sonnet 5 by default, flagged tickets to Fable 5) as unnecessary complexity — note it is implementable only on the workflow path, since `agent()` takes `model` *and* `effort` while the Agent tool behind `/review-ticket` takes `model` only; and shrinking tickets further to reduce the diff surface, declined at the current scale. | Pricing (incl. the dated Sonnet 5 introductory rate), model positioning, comparative latency, and the `high` effort default on Claude Code: `[official]` — [models overview](https://platform.claude.com/docs/en/about-claude/models/overview) and [effort](https://platform.claude.com/docs/en/build-with-claude/effort), both verified 2026-07-29. Reviewer quality at this tier: **no benchmark claimed in either direction**; the maintainer's cross-project experience is `[internal]` — a report, with no harness, project, or numbers recorded. Model and effort choice: `[team-policy]` — cost-driven, **still unmeasured** (see §4) |
| Triage (nightly only) | Claude Sonnet 5 | `xhigh` | Classification + ticket synthesis is planning-shaped work; profile derived separately from the Architect row (nightly triage does not need the Architect's full planning tier). | `[team-policy]` — no separate benchmark basis |

Figures dated 2026-07-17 are from the team-finalized specification (§7); the Reviewer row was revised 2026-07-21, the Architect row 2026-07-22, both Opus-tier rows 2026-07-26 on the Opus 5 release, the Reviewer effort again 2026-07-28, and the Reviewer's model + effort together 2026-07-29 (§7). Do not re-derive or silently alter them; update only via the process in the repo CLAUDE.md (new as-of date + provenance entry in the same commit).

## 4. Known failure modes / pitfalls

| Pitfall | Context | Mitigation | Recorded |
|---|---|---|---|
| **Reviewer contamination** — review run inside the Builder's session inherits the Builder's assumptions and blind spots | Design constraint from the pattern spec (not a measured incident) | Reviewer always starts in a fresh context; `/review-ticket` checks for and refuses a contaminated session | 2026-07-17 |
| **Tier collapse** — Reviewer switched to the Builder's model/tier "to save cost" | Design constraint | Different tier is a hard requirement; changing it means updating this pattern entry first, not the target repo's config | 2026-07-17 |
| **Self-clearing** — Builder declares its own "small" diff done and skips review | Process risk | No merge without a Reviewer verdict attached to the PR | 2026-07-17 |
| **Unbounded bounce loop** — Reviewer ↔ Builder ping-pong without convergence | Process risk | Cap at 2 bounce cycles, then escalate to a human | 2026-07-17 |
| **Cold-start starvation** — the fresh-context Reviewer (or Builder) lacks context that lived only in the Architect's conversation | Design implication of fresh contexts | Ticket and plan must be self-contained (cold-startable); a plan that needs the planning conversation to be understood is defective | 2026-07-17 |
| **`max`-effort overthinking / latency at the gate** | Claude Code's own effort-level description: `max` "may use excessive tokens resulting in long response times or overthinking. Use sparingly." — `[official]` product text, observed 2026-07-17 | Accepted at this gate by explicit `[team-policy]`; do not copy `max` to Builder/Architect by default; keep tickets small so review diffs stay bounded | 2026-07-17 |
| **Orchestrator role leakage** — the main (orchestrator) session absorbs subagent work: plans, implements, or reviews inline instead of dispatching, dissolving the role boundaries the pattern exists for | `[internal]` — observed on fx-eye-tracking's planner/generator/evaluator harness (sibling of this pattern), reported by the maintainer, 2026-07-17. Soft "launch the subagent" prompts alone did not hold. | Mechanically enforced since 2026-07-17: a PreToolUse guard denies main-session Edit/Write while subagent calls pass (`agent_id` is present in hook input only for subagents — see the verification record in `scaffold/INSTALL.md`). Backed by prose rules in the CLAUDE.md snippet and a "never absorb the role" line in every stage command. If leakage still recurs, switch to Mode B (separate human-run sessions — no orchestrator exists to leak). | 2026-07-17 |
| **Silent delivery drop** — end-of-pipeline bookkeeping relied on side effects that never fired: many MRs merged, **zero** tracker issues closed, and no step verified the transition | `[internal]` — fx-eye-tracking, reported by the maintainer, 2026-07-17. Root cause on that instance not yet diagnosed; typical causes: missing `Closes #N` in the MR description, or merging to a non-default branch (GitLab auto-close fires only on default-branch merges). | Delivery is verified, not assumed: run `/verify-delivery <ticket>` after every merge — it checks the Definition of Done (plan · tests · CLEAR verdict · merged · issue closed · writeback) and repairs gaps only with explicit human OK. Never trust tracker auto-close blindly. | 2026-07-17 |
| **No PR/MR ever created — review trail never leaves the machine** — the deterministic deliver step merged the branch locally and pushed, opening no PR/MR: 19 delivered tickets produced 0 PRs / 0 remote branches on the forge, the "CLEAR verdict attached to the MR/PR" DoD item was structurally unsatisfiable, and CI (`on: push`) ran only after the merge already landed | `[internal]` — Ruihang2017/Groundwork (adopted repo), Windows, 2026-07-18 (catalog issue #50, filed by the maintainer through the upstream feedback channel) | Fixed 2026-07-18 (§7): PR-mode delivery — push the branch, open a PR/MR, post the CLEAR verdict as a comment, merge **through the forge** (`gh pr merge` / `glab mr merge` — a required-but-unmet check now escalates instead of force-landing), then close + verify. Direct-merge kept as the no-forge fallback. `/verify-delivery` item 2 is now mechanically checkable | 2026-07-18 |
| **Guard bypass via Bash** — the write guard blocks the Edit/Write tool family only; the main session can still modify files through Bash (`echo >`, `git apply`, heredocs) | Known boundary of the mechanical guard — design analysis, 2026-07-17, not an incident | Accepted deliberately: blocking Bash would break legitimate orchestrator operations (checkout, merge, tracker CLI). The guard targets the observed failure mode — reflexive "I'll just edit it myself" — not a determined bypass. Prose rule stands: Bash in the main session is for orchestration, never for writing files. If bypass is ever observed in practice, record it here and escalate to Mode B. | 2026-07-17 |
| **Nightly grinding** — the sweep re-attempts the same unfixable issue night after night, burning tokens | Design risk of the nightly loop (not a measured incident) | Unsolved/failed issues get `nightly:escalated` and are excluded from later sweeps until a human clears the label; `maxIssues` caps each night's spend | 2026-07-17 |
| **Autonomous misjudgment of "invalid"** — the sweep wrongly declares a real issue invalid and it gets ignored | Design risk | Invalid verdicts only **label** (`triage:invalid`) and comment with evidence — never auto-close; the morning human is the final judge; triage is instructed to prefer `needs-human` when uncertain | 2026-07-17 |
| **CRLF workflow scripts rejected on Windows** — the Workflow tool's permission layer refuses scripts containing `\r` ("control characters that would be hidden in the approval dialog"), so `/start-milestone` and `/nightly-issues` cannot launch their workflows when git autocrlf has rewritten the installed `.js` files | `[internal]` — **first field observation**: Ruihang2017/Groundwork, Windows 11, Claude Code, 2026-07-18 (catalog issue #21; workaround used: inline `script` param with LF) | Fixed 2026-07-18: catalog ships `.gitattributes` (`* text=auto eol=lf`), `adopt.mjs` normalizes all text installs to LF regardless of source state, and E2E guards assert no `\r` in runtime files (scaffold + installs + catalog self-host) | 2026-07-18 |
| **CRLF regression on target checkouts** — issue #21's install-time LF normalization does not survive the target repo's own git: a later `checkout`/clone with `core.autocrlf=true` re-CRLFs the installed workflow scripts, and `Workflow({name: …})` (including `start-all`'s name-resolved `run-milestone` child) is rejected again for control characters | `[internal]` — recurrence: Ruihang2017/Groundwork, Windows 11, Claude Code, 2026-07-18 (catalog issue #23; workaround used: inline `script` param with LF) | Fixed 2026-07-18: `adopt.mjs` installs/appends a `.gitattributes` in the **target repo** pinning `.claude/workflows/*.js` and `.claude/scripts/*.mjs` to `text eol=lf` (marker-checked, append-once); E2E asserts fresh install, append-to-existing, and idempotent re-run | 2026-07-18 |
| **Workflow `args` arrive JSON-stringified** — some harness paths deliver the Workflow tool's `args` as a JSON **string**, so `Object.assign(defaults, args)` merges nothing and launch validation throws in milliseconds (`args.tickets must be a non-empty array`, `args.modules must be a non-empty array`) | `[internal]` — Ruihang2017/Groundwork, Windows 11, Claude Code, 2026-07-18 (catalog issue #23; killed the first `run-milestone` launch in 10ms and would crash `/start-all` identically) | Fixed 2026-07-18: all three workflow scripts (`run-milestone.js`, `start-all.js`, `nightly-issues.js`) parse first — `typeof args === 'string' ? JSON.parse(args) : (args \|\| {})`; E2E runs runner + start-all scenarios with stringified args end-to-end | 2026-07-18 |
| **Intra-module tickets dispatched out of dependency order** — `milestone-dag.mjs` emitted each module's tickets in ID (filename) order, not topologically by their own `blocked_by` edges; when a module's declared order deviates from ID order, run-milestone dispatched a ticket before the sibling it is blocked_by, the Builder stopped at its plan's precondition gate, and the whole module failed (chain-blocking its dependents) | `[internal]` — Ruihang2017/Groundwork2, autonomous `/start-all`, 2026-07-18 (catalog issue #31; module `05-jobs-fit`, `0502 blocked_by 0503` but ID order put 0502 first) | Fixed 2026-07-19: `milestone-dag.mjs` topologically sorts tickets within each module by intra-module `blocked_by` (cross-module edges stay module-level; ID order only as tiebreak), with an intra-module cycle failing loudly like the module-level one. Same Kahn sort now serves both levels. | 2026-07-19 |
| **Architect and Reviewer share a model** (both Fable 5 during the 2026-07-22 trial) — the Reviewer is blinder to "the plan itself was wrong" than when the two differed, because it shares the planner's blind spots about the plan | Accepted design cost of the Architect trial (catalog issue #48), 2026-07-22 — not an observed incident | **Resolved 2026-07-26** by ending the trial: the Architect moved to Opus 5, so all three roles now run distinct models and the Reviewer shares blind spots with neither the planner nor the implementer. Note the exposure was never actually measured — the trial recorded no plan-level defects because it recorded nothing. Kept as a standing caution: any future re-pin that collapses Architect and Reviewer onto one model re-opens this row | 2026-07-22 (resolved 2026-07-26) |
| **Architect and Builder share a model** (both Opus 5 since 2026-07-26) — the plan and the implementation come from one model's priors, so a blind spot in how the work was framed carries into how it was built | Accepted design cost of the 2026-07-26 Opus 5 re-pin — not an observed incident | Deliberately the lesser of the two shared-model configurations: §2's hard requirement is that the **judge** differs from the implementer, and the Reviewer (Sonnet 5 since 2026-07-29) differs from Architect and Builder alike. Watch for the Reviewer clearing work that satisfies a plan both upstream roles read the same wrong way; if that appears, split Architect and Builder again | 2026-07-26 |
| **Auto-created MR/PR body ignored the repo template** — `deliver-ticket.mjs` hardcoded a 3-section body, so an adopted repo's 8-section `.gitlab`/`.github` template (incl. the Constraint-check audit) never applied to pipeline MRs, and `docs/plans/*.md` (the Architect's untracked plan) tripped the clean-tree guard on every delivery | `[internal]` — Ruihang2017/Groundwork, 2026-07-23 (catalog issue #58) | Fixed 2026-07-23 (§7): the deliver agent fills the repo MR/PR template from ticket+diff+verdict+CLAUDE.md and passes `--body-file`; deliver-ticket resolves `--body-file` → repo template skeleton → hardcoded fallback, and ignores `docs/plans/` in its clean-tree check; `adopt` git-ignores `docs/plans/*.md`. | 2026-07-23 |
| **MR/PR API forbidden by org token policy** — a GitLab token with a working Issues API but a **403 MR API**, on a repo whose default branch requires MRs: PR mode cannot create/merge the MR, and direct mode cannot push the protected default — neither delivery mode completes | `[internal]` — Ruihang2017/Groundwork (GitLab), 2026-07-23 (catalog issue #56) | Fixed 2026-07-23 (§7): `pushmr` sub-mode opens the MR over SSH via `git push -o merge_request.*` (no MR API), single-line `Closes #N` description, CLEAR verdict as an issue comment (Issues API), stop for a human web-merge, resume delivers via the Issues API. Auto-detected by a cheap MR-API probe. | 2026-07-23 |
| **Module barrier serializes independent modules** — the old `start-all` awaited each `run-milestone`, so two modules with *no* dependency between them still ran one after the other | `[internal]` — measured by `dag-report.mjs` on catalog fixtures, 2026-07-27 (issue #68). On a 19-ticket / 5-module fixture at the recommended concurrency of 4: **11 ticket-rounds vs 5 globally scheduled — 55% lost to the barrier** | **Fixed 2026-07-27** (issue #71): `/start-all` schedules every ticket from the flat DAG, so only `blocked_by` gates. Still applies to **`/start-milestone`**, which is one module by definition — `dag.html` prices the difference per PRD. Do not "fix" it by widening modules: module boundaries are cut on file-ownership, and that is what keeps concurrent lanes from colliding at merge | 2026-07-27 (fixed same day) |
| **A mid-run DAG change the scheduler cannot honor** — `/start-all` reloads the DAG while running, so a ticket can gain a `blocked_by` *after* it has already been dispatched. That edge cannot be applied retroactively | Design consequence of the dynamic DAG (issue #71), 2026-07-27 — not an observed incident | The run does not pretend: the edge is **not** applied, the ticket keeps its real outcome, and an entry lands in the run's `escalations` naming the ticket and both dependency sets. Aborting instead would discard in-flight work; silently rewriting deps would misreport what the run enforced. `/start-all` step 7 requires relaying `escalations` verbatim — a summary that drops them hides exactly the cases needing a human. Same channel reports mid-run cycles, failed reloads, and hit runaway guards (`maxTickets`, `maxRescans`) | 2026-07-27 |
| **The wave model over-predicts speed-up** — `dag.html`'s wave count is a ceiling, not a forecast | `[internal]` — catalog Level-1 rehearsal, 2026-07-27 (issue #75). Two isomorphic 6-ticket sets, identical DAG shape, only concurrency differing. Model predicted 6 waves → 2 waves = **3.0×**; measured **104.3 min → 47.2 min = 2.21×**, a 36% over-prediction. Both runs 6/6 delivered, 0 bounces | Two structural causes, neither visible to the model: ticket durations vary so a wave is paced by its **slowest** lane; and **deliver is serialized behind a mutex**, so lanes that finish building together then queue to merge — a ceiling that tightens as lanes increase. Quote the wave count as an upper bound. Parallelism also costs **+13.8% tokens** (worktree setup and more tool calls), so `concurrency > 1` buys wall-clock with spend, not for free | 2026-07-27 |
| **A long autonomous run dies with the session** — background workflows are bound to the Claude Code process; if it exits mid-run the run stops silently, with no escalation and no completion record | `[internal]` — catalog Level-1 rehearsal, 2026-07-27 (issue #75): a `/start-all` launched at concurrency 4 was killed at launch when the previous process exited. Runs here take 47–104 minutes, so the window is not small | The stop is **safe** by construction — deliver's order is merge → push → close issue, so a mid-flight death leaves unmerged ticket branches and still-open issues, which the closed-issue resume filter correctly treats as undelivered. Recovery depends on intent: to continue work, resume (deliver is idempotent, Level-0 D2); **to measure anything, re-run clean** — `resumeFromRunId` replays completed agents from cache, so a resumed run's wall-clock is fiction | 2026-07-27 |
| **The Reviewer's effort was cut for cost, and nothing measured the consequence** — `xhigh` → `high` on 2026-07-28 because Fable 5 is the priciest model in §3 ($10/$50 per MTok, 2× Opus 5) | `[team-policy]` — maintainer decision, 2026-07-28 (issue #105). **No number supports it in either direction**: effort scales thinking volume, which varies per task, so the saving is unquantified; and the Level-1 rehearsal cleared 12/12 tickets with **0 bounces**, so it produced no evidence about reviewer strength — nothing needed catching | Watch the **bounce rate**: a gate that stops bouncing is not necessarily a gate that stopped finding things. If defects start reaching merge, raise effort back to `xhigh` before reaching for a different model. The step deliberately stopped at `high` (Anthropic's default) rather than `medium`/`low`, which the same guidance scopes to "routine work" — a merge gate is not routine. **Superseded 2026-07-29** (issue #111): the effort cut existed only to make Fable 5 affordable; the model changed instead, and effort went back to `xhigh`. The row stays as the record of a cost decision taken with no measurement — the same gap the row below now carries | 2026-07-28 (superseded 2026-07-29) |
| **The judge runs a lower tier than the implementer** — the Reviewer (Sonnet 5) reviews the Builder's work (Opus 5). §2's independence requirement is intact (different models, so different blind spots), but the gate no longer has a capability margin over the work it judges, and a defect both models are equally blind to now has nothing above it | `[team-policy]` — maintainer decision, 2026-07-29 (issue #111), driven by cost: Fable 5 at $10/$50 per MTok was judged unaffordable at real run volume. Supporting evidence is `[internal]` and thin — the maintainer reports acceptable results from a Sonnet 5 reviewer on other projects, with **no harness, project, or numbers recorded**. Not an observed failure here | `xhigh` effort partly compensates (a step up from Sonnet 5's `high` default). The real mitigation is **measurement, which has not been done**: the Reviewer row has now changed three times in nine days (2026-07-21, 2026-07-28, 2026-07-29) with zero data collected on any of them. `testbed/rehearsal/` is the instrument that could settle it — two isomorphic ticket sets, identical DAG, only the reviewer pin differing, compared on bounce rate and on defects that reach merge. Until that runs, treat this row as an open exposure, not an accepted cost. Watch specifically for **CLEARs on work that merely satisfies the plan**: the Architect and Builder share a model (row above), so the Reviewer is now the only role outside that pair *and* the weakest of the three | 2026-07-29 |
| **Harness-specific timeouts/failures** for these exact model+effort combinations | **None recorded yet** (as of 2026-07-17) | When observed: record here with harness name, conditions, and date | — |

## 5. Upstream / downstream integration

**Upstream (work intake)** — assumes the standard docs layout (exemplar: fx-eye-tracking):

- `docs/PRD.md` (master PRD) → `docs/prd/<module>/README.md` (sub-PRD) → `docs/prd/<module>/tickets/*.md` (one file per ticket; each ticket is a future issue body).
- **Later phases split the document, never the tree** (issue #112). Work added after a Gate 2 gets its own PRD — `docs/PRD-02-<name>.md`, passed to `/breakdown-prd <path>` — decomposed into the **same `docs/prd/`**. One root is not a convention but a constraint: `dag-scan.mjs`, `dag-report.mjs` and `/start-all` each take exactly one, and a cross-phase `blocked_by` resolves only inside one graph (a dangling ref is a hard failure by design). A parallel `docs/prd2/` would forfeit the single global DAG of issue #71 for nothing. In append mode `prd-phase.mjs context` supplies the next module prefix, the ticket ids already in use, and the existing-file list as **data**, and `prd-phase.mjs check` then enforces against git that pre-existing files under `docs/prd/` were only **added to** — never modified or deleted, because a delivered ticket is the record of what was built and `/start-all` cannot re-run it anyway. `--redo` is the explicit escape hatch for a pre-Gate-1 decomposition that was simply wrong. Running the new phase is just `/start-all` again: the earlier phases' issues are closed and filter out.
- The **ticket file is the Architect's input**; the Architect also reads the linked sub-PRD and any `docs/adr/` entries touching the affected area.
- The Architect's plan lands at `docs/plans/<ticket-id>.md`. A hard-to-reverse choice made while planning becomes a new ADR, not a paragraph buried in the plan.

**Downstream (deliverables):**

- Builder → an implementation on the ticket branch: code + passing tests, plus a deviations note where it departed from the plan.
- Reviewer → a verdict: **CLEAR** (short note of what was checked) or **BOUNCE** (numbered findings: `file:line`, failure scenario, severity). Findings go back to the Builder.
- Deliver step (deterministic, `deliver-ticket.mjs`) → the durable artifacts on the forge: the ticket branch pushed, a **PR/MR** carrying `Closes #N` + the plan, the Reviewer's **CLEAR verdict posted as a comment**, the PR/MR **merged through the forge** (branch protection respected), and the tracker issue closed-and-verified. No remote/forge → local `--no-ff` merge fallback; GitLab with a 403 MR API + protected default branch → `pushmr` (push-option MR over SSH, verdict as an issue comment, human web-merge) — issue #56. The MR/PR body is filled from the repo's own `.gitlab`/`.github` template (the deliver agent fills Type/Changes/Constraint-check/Evidence from the ticket + diff + verdict + CLAUDE.md; `--body-file` → template skeleton → hardcoded fallback), not a fixed 3-section stub — issue #58.
- Net output: merged PR/MR with the review trail attached, updated docs/ADRs, closed ticket. Merge requires a CLEAR verdict.
- **Feedback channel (upstream):** pattern-level problems observed while running the pipeline — a role boundary that misfits, a stale model/effort pin, an orchestration bug — are filed as issues against the pattern catalog (`Ruihang2017/agent-templates`, issue templates provided); the catalog self-hosts the nightly sweep and triages them. Project-level bugs stay in the project's own tracker.
- Delivery is **verified, not assumed**: after every merge, `/verify-delivery <ticket>` checks the Definition of Done — plan on disk · tests green · **CLEAR verdict attached to the PR/MR as a comment** · MR/PR merged into the default branch · tracker issue closed · writeback done. Added 2026-07-17 after the fx-eye-tracking silent-delivery-drop observation (§4); the verdict-attached item became mechanically real with PR-mode delivery (§4, issue #50).

**Human gates (target operating model):**

- **Gate 1 — upstream sign-off = the start signal:** once the module's sub-PRD and all its tickets are generated (from a bare PRD: `/breakdown-prd` generates them for review; format: the catalog's `templates/ticket.template.md`), a human types **`/start-milestone <module>`**. That one action is the sign-off. `/breakdown-prd` also writes **`docs/prd/dag.html`** (`dag-report.mjs`) so the reviewer can see the *execution shape* of the decomposition before signing it, not just the ticket list: **one flowchart of every ticket in the PRD**, colored by module, with the **recommended `concurrency`** to pass to `/start-all` derived from the ticket DAG rather than guessed. A module the page flags **fully serial** is a file-scope decomposition problem, and Gate 1 is the last cheap moment to fix it. The page renders two schedules over that one graph — *as the runner executes* (module barriers respected, which is what `/start-all` does today) and *globally scheduled* (only `blocked_by` gates) — and states the rounds lost to the barrier. The runner cannot execute the global schedule; that number is there to **price** the barrier, not to promise a speed-up (§4). The session then publishes every ticket as a tracker issue (`.claude/scripts/publish-tickets.mjs` — deterministic and idempotent, `[<id>]` title prefix as the dedupe key; agents never hand-create issues) and launches the milestone runner.
- **Gate 2 — smoke test:** a human smoke-tests the delivered work at the end of the ticket batch / milestone. **Per phase, not once per project** (issue #112): a project that adds a PRD phase after Gate 2 runs both gates again over the new document. The two-gate model bounds a *delivery*, it does not bound the project's lifetime.
- **The resume filter must report itself.** Closed-issue filtering is what makes re-runs — after a crash, a supervised pause, or an appended phase — safe, but a filter that drops work silently is indistinguishable from work that ran. `publish-tickets.mjs` returns each ticket's issue `state` in its summary, and `/start-all` relays what it skipped. A ticket whose text no longer matches its **closed** issue comes back as `drift`: either it was edited after delivery (a human decides whether to re-run it) or the issue predates a body-format change (`--sync` fixes that one). Either way it escalates; the scheduler never resolves it silently (issue #112).
- **Between the gates the pipeline runs autonomously** — `.claude/workflows/run-milestone.js` for a single module (`/start-milestone`, nightly sweep), `.claude/workflows/start-all.js` for the whole PRD. Per ticket both do: plan → build → review → on CLEAR, deliver = open PR/MR + verdict comment + forge-merge + close issue, with no per-ticket human approval. Stage order, reviewer freshness, the bounce cap, and no-merge-without-CLEAR are enforced **in code**, not prose — prose-only orchestration is a recorded failure mode (§4).
- **`/start-all` schedules from one flat DAG, and that DAG is live** (issue #71). It is not a sequence of `run-milestone` calls: every ticket in the PRD is gated only by `blocked_by`, so independent modules overlap. Every `rescanEvery` settled tickets — and always once more before finishing — it re-reads `docs/prd` through an agent, so **a ticket added while the run is in flight is published, scheduled, executed, and re-rendered into `dag.html`**. Workflow scripts cannot touch the filesystem, so that agent hop is the mechanism, not a convenience. What the scheduler *cannot* honor (a dependency added to an already-dispatched ticket, a cycle introduced mid-run, a failed reload, a hit runaway guard) is reported in `escalations` rather than silently applied — see §4.
- **Exception path (the only other way a human appears):** 2 bounce cycles without convergence, a failed build, or an unrepairable delivery item escalates to a human; the runner is fail-fast by default because later tickets may depend on earlier ones.
- **On-ramp:** a project newly adopting the pattern starts in `supervised` mode: the runner takes one ticket to a CLEAR verdict, **opens a PR/MR** (plan + verdict comment) and **stops the run** for the human to review + merge (later tickets may depend on the merged result). Re-run `/start-milestone` to continue — tickets whose issues are already closed are filtered out before the run, which also makes crash recovery mechanical. Switch to `autonomous` once the pattern holds. The mode is declared in the target repo's CLAUDE.md (see `scaffold/claude-md-snippet.md`).

**Orchestration mechanics:** a single ticket can still be run by hand with the stage commands; a milestone runs through the deterministic workflow. `ultracode` is not needed in target repos — typing `/start-milestone` is the explicit orchestration request (its instructions call the Workflow tool). Note that `ultracode` is a Claude Code **session setting** (`xhigh` effort + automatic workflow orchestration), not a model effort level — the pattern's pinned per-role efforts are unaffected by it. `[official]` — workflows + model-config docs, verified 2026-07-17 (record in `scaffold/INSTALL.md`).

**Whole-PRD driver (`/start-all`):** one sign-off runs every ticket in the PRD, scheduled from the flat `blocked_by` DAG (`dag-scan.mjs`; dangling refs and cycles are hard errors). Since 2026-07-27 it is **its own scheduler, not a sequence of `run-milestone` calls** (issue #71) — independent modules overlap instead of queueing. Failure policy (maintainer decisions, catalog issue #20, now applied per ticket rather than per module): a failed ticket cascades to its dependents; independent DAG branches continue in `autonomous`; anything short of a CLEAR stops everything in `supervised`; re-runs resume via closed-issue filtering (everything already delivered arrives filtered → `already-complete`). No cost ceiling by explicit `[team-policy]`. **`/start-milestone` is deliberately kept** for one module at a time — a different risk posture (module-boundary checkpoints during a fresh adoption, or uneven confidence across modules), not a redundant entry point.

**Parallel lanes (`concurrency`, opt-in):** `run-milestone`/`start-all` take a `concurrency` arg (default **1 = fully sequential, unchanged**). `>1` (autonomous only — supervised is forced to 1) runs independent tickets WITHIN a module as **parallel lanes**, DAG-scheduled from ticket `blockedBy`: each lane's builder + reviewer run in **isolated git worktrees** (the reviewer detached-checkouts the builder's commit — validated: a committed branch persists in the shared `.git` after worktree cleanup, and disjoint file-scopes make concurrent worktree builds + serialized merges clean), the architect writes the plan on the main tree and its **content is passed to the isolated builder** (a worktree can't see the gitignored plan), and **deliver is serialized** (merges to the default branch never overlap; a hidden file-scope overlap surfaces as a merge conflict → abort → escalate). Real parallelism is bounded by the DAG shape and the harness's `min(16, cores−2)` concurrent-agent cap, and it multiplies token spend — so it is opt-in per run. Under **`/start-all`** lanes are drawn from the whole PRD, so independent modules overlap (issue #71); under **`/start-milestone`** they are intra-module by definition, which is what the module barrier line in `dag.html` prices.

**Nightly issue sweep (unattended variant):** `claude -p "/nightly-issues"` on an OS schedule (Windows Task Scheduler etc.) while the machine is on. Flow: collect open issues → **Triage** (read-only; synthesizes tickets under `docs/prd/99-nightly/` for fixable ones) → **run-milestone** (autonomous, `continueOnFailure` — issues are independent) → one **report** step does all tracker writes: per-issue outcome comments, labels (`triage:invalid` / `nightly:escalated` / `needs-human`), closes delivered issues, and a `Nightly report YYYY-MM-DD` issue. The morning email is the tracker's own notification stream (watch the repo) — no SMTP to configure. Cost cap: `maxIssues` per night (default 5). Hand-written issues follow the catalog's universal tracker templates (`templates/tracker/`, installed to `.github/` / `.gitlab/`) so triage can convert them mechanically. Headless flags, permissions, and scheduling: `scaffold/INSTALL.md` § Nightly sweep.

## 6. Scaffold

```
scaffold/
├── INSTALL.md                 # install steps + config-key verification record
├── claude-md-snippet.md       # block to append to the target repo's CLAUDE.md
└── .claude/
    ├── settings.json          # wires the PreToolUse write guard
    ├── hooks/
    │   └── guard-main-session-writes.mjs  # denies main-session Edit/Write; subagents pass
    ├── scripts/
    │   ├── publish-tickets.mjs # tickets → tracker issues; idempotent; dry-run by default
    │   ├── dag-core.mjs      # shared graph core: frontmatter parse, topo sort, wave simulation
    │   ├── milestone-dag.mjs  # module DAG from ticket blocked_by frontmatter (topo order, cycle detection)
    │   ├── dag-scan.mjs       # flat ticket graph — start-all's scheduler input + mid-run DAG reload
    │   ├── prd-phase.mjs      # phased PRDs: `context` (next prefix, used ids, frozen files) + `check` (freeze, git-enforced)
    │   ├── dag-report.mjs     # Gate 1 view: docs/prd/dag.html — whole-PRD flowchart, lanes, concurrency
    │   └── deliver-ticket.mjs # PR/MR + verdict comment + forge-merge + close; direct-merge fallback
    ├── workflows/
    │   ├── run-milestone.js   # one module's tickets (Workflow script) — /start-milestone + nightly
    │   ├── start-all.js       # whole-PRD scheduler over the flat DAG; reloads the DAG mid-run
    │   └── nightly-issues.js  # nightly sweep: triage → pipeline → report (Workflow script)
    ├── agents/                # role definitions with pinned model + effort
    │   ├── architect.md       # claude-opus-5 @ max; writes the plan, no production code
    │   ├── builder.md         # claude-opus-5 @ xhigh; inherits all tools
    │   ├── reviewer.md        # claude-sonnet-5 @ xhigh; read/run-only (no Write/Edit)
    │   └── triage.md          # claude-sonnet-5 @ xhigh; nightly classification + ticket synthesis
    └── commands/
        ├── breakdown-prd.md   # /breakdown-prd [notes]    → pre-Gate-1: PRD → sub-PRDs + tickets
        ├── plan-ticket.md     # /plan-ticket <ticket>    → Architect stage
        ├── build-ticket.md    # /build-ticket <ticket>   → Builder stage
        ├── review-ticket.md   # /review-ticket <ticket>  → Reviewer stage (fresh context)
        ├── verify-delivery.md # /verify-delivery <ticket> → post-merge Definition-of-Done check
        ├── start-milestone.md # /start-milestone <module> → Gate 1 signal: publish issues + run milestone
        ├── start-all.md       # /start-all [mode]         → Gate 1 for the whole PRD (DAG order)
        └── nightly-issues.md  # /nightly-issues [max]     → unattended sweep (headless claude -p)
```

Adoption is one command from the catalog — `node scripts/adopt.mjs three-agent-architect-builder-reviewer <target-dir>` — see the catalog's `ADOPTING.md` for the end-to-end walkthrough (bare `PRD.md` → running pipeline).

Shared, universal assets live at the **catalog root**, not in this scaffold: `templates/ticket.template.md` (the ticket format — field-proven fx / PIL-15 / FND-9 standard) and `templates/tracker/` (issue + PR/MR templates for GitHub and GitLab). INSTALL steps 1–2 copy them into the target repo alongside `.claude/`.

The deterministic chain is covered by the repo's Level-0 E2E (`node testbed/e2e/run-e2e.mjs`, see `testbed/README.md`) — green is the merge gate for scaffold changes. Level 1 (live pipeline rehearsal on `testbed/app/`) is the promotion trigger from `trialed` to `adopted`.

Install steps and usage modes: [scaffold/INSTALL.md](scaffold/INSTALL.md). Frontmatter keys (`model`, `effort`, `tools`, command arguments) verified against official Claude Code docs on 2026-07-17 — see the verification record in INSTALL.md.

## 7. Provenance & change log

| Date | Change | Basis | Author |
|---|---|---|---|
| 2026-07-17 | Initial entry. Roles, boundaries, and the model/effort table adopted as the team standard. | Team-finalized specification, 2026-07-17. Benchmark figures transcribed as recorded there; original source links were not captured — attach them at the first re-verification. Scaffold config keys verified against live Claude Code docs same day (record in `scaffold/INSTALL.md`). | Horace Hou (spec) / Claude Fable 5 (write-up) |
| 2026-07-17 | Added two `[internal]` failure modes observed on the fx-eye-tracking sibling harness: orchestrator role leakage, and silent delivery drop (MRs merged, issues never closed). Hardened the scaffold accordingly: orchestrator-discipline rules in the CLAUDE.md snippet, "never absorb the role" line in every stage command, new `/verify-delivery` Definition-of-Done command. Model/effort table unchanged. | Maintainer report (Horace Hou), 2026-07-17. | Horace Hou (report) / Claude Fable 5 (write-up) |
| 2026-07-17 | Documented the target operating model — two human gates (upstream PRD/sub-PRD/ticket sign-off; final smoke test), autonomous pipeline in between, `supervised` on-ramp mode. Orchestrator write-denial is now mechanically enforced: PreToolUse guard denies main-session Edit/Write, subagent calls pass. Model/effort table unchanged. | Maintainer direction (Horace Hou), 2026-07-17. Hook mechanism verified against live Claude Code docs the same day (hooks.md, permissions.md — record in `scaffold/INSTALL.md`). | Horace Hou (direction) / Claude Fable 5 (write-up) |
| 2026-07-17 | Gate 1 made concrete: `/start-milestone` = the human start signal → tickets auto-published as tracker issues (`publish-tickets.mjs`, adapted from fx-eye-tracking `create-issues.mjs`; `[<id>]` title prefix as dedupe key; smoke-tested dry-run/no-CLI/usage paths) → deterministic milestone runner (`run-milestone.js` Workflow script: stage order, reviewer freshness, bounce cap, merge policy in code). Added `templates/ticket.template.md` (adapted from fx ticket discipline). Recorded the Bash guard boundary as a known pitfall. Clarified `ultracode`: a session setting (`xhigh` + automatic workflow orchestration), not a model effort level — role efforts unchanged. | Maintainer direction (Horace Hou), 2026-07-17. fx-eye-tracking conventions read the same day (read-only). Workflow tool, `.claude/workflows/`, and `ultracode` semantics verified against live docs (workflows, claude-directory, model-config — record in `scaffold/INSTALL.md`). | Horace Hou (direction) / Claude Fable 5 (write-up) |
| 2026-07-18 | **`/start-all` shipped** (catalog issue #20; maintainer-decided semantics: failed module blocks dependents, independent branches continue in `autonomous`, any non-CLEAR stops everything in `supervised`, no cost cap, resume via closed-issue filtering): deterministic DAG script (`milestone-dag.mjs`, ticket `blocked_by` as ground truth, cycles/dangling refs are hard errors) + `start-all` workflow composing `run-milestone` per module + `/start-all` command. 19 new E2E checks (DAG fixtures incl. cycle; module-policy scenarios via stubbed child workflows). Per-ticket stages unchanged. | Maintainer answers on issue #20, 2026-07-18. | Horace Hou (decisions) / Claude Fable 5 (build) |
| 2026-07-18 | **First field bug fixed** (catalog issue #21, reported from Ruihang2017/Groundwork through the upstream feedback channel): CRLF-rewritten workflow scripts are rejected by the Workflow tool on Windows. Fix: repo-wide `.gitattributes` (`eol=lf`), working tree renormalized (67 files), `adopt.mjs` installs all text as LF, E2E `\r` guards added. Recorded as the pattern's first true `[internal]` pitfall observation. | Field report (Horace Hou via Groundwork), 2026-07-18. | Horace Hou (report) / Claude Fable 5 (fix) |
| 2026-07-29 | **Reviewer moved to Claude Sonnet 5 @ `xhigh`** (catalog issue #111, maintainer decision). Fable 5 leaves the table: at **$10/$50 per MTok** it is 2× Opus 5 and 3.3× Sonnet 5 ($3/$15 — $2/$10 introductory through **2026-08-31**, so ~5× cheaper today and 3.3× from September), and the maintainer's position is that it is unaffordable at real run volume, with a Sonnet 5 reviewer having performed acceptably on other projects. This is the **same substitution offered and declined on 2026-07-28** (§7 previous entry: "Sonnet 5 @ `xhigh` was offered as the cheaper *and* §2-safe alternative and declined, to keep the last gate on the strongest model") — accepted now on cost. The 2026-07-28 effort cut is therefore **reverted**: `high` existed only to make Fable 5 affordable, so with the model changed, effort goes back to `xhigh`, a deliberate step up from Sonnet 5's documented `high` default. **§2 holds and is now stated explicitly:** the requirement is a *different* model, not a *stronger* one — the mechanism is blind-spot independence, which is direction-agnostic — so a new §2 paragraph says so outright, because "different tier" was previously readable as "the gate is the strongest model", which is no longer true. This is **not** the rejected Opus 4.8 proposal (that was the Builder's own tier = §4 tier collapse); Sonnet 5 keeps all three roles on distinct models. **Recorded honestly as an exposure, not an accepted cost** (new §4 row): it is the first configuration where the judge runs a lower tier than the implementer, the supporting evidence is `[internal]` with no harness/project/numbers, and the Reviewer row has now changed **three times in nine days (07-21, 07-28, 07-29) with zero measurement on any of them** — `testbed/rehearsal/` is named as the instrument that could settle it. Secondary, unmeasured: Sonnet 5's comparative latency is "Fast" vs Fable 5's "Slower", on a role that sits on every ticket's critical path. Rejected by the maintainer: risk-tiered review (workflow-path-only, since `agent()` takes `effort` and the Agent tool behind `/review-ticket` does not) and further ticket shrinking. Same commit: §2 clarification, §3 row + §3 as-of, metadata As-of/expiry, §4 (new row + prior row marked superseded + Fable reference in the shared-model row), §6 scaffold-tree pin, snippet, agent frontmatter (scaffold + self-hosted copy), integrity pins, site-suite model assertion. | Maintainer decision (Horace Hou), 2026-07-29. Pricing (incl. the dated Sonnet 5 introductory rate), positioning, comparative latency, and the `high` effort default on Claude Code verified same day against live docs: [models overview](https://platform.claude.com/docs/en/about-claude/models/overview), [effort](https://platform.claude.com/docs/en/build-with-claude/effort). No reviewer-quality benchmark claimed in either direction. | Horace Hou (decision) / Claude Opus 5 (build) |
| 2026-07-28 | **Reviewer effort cut for cost: `xhigh` → `high`** (catalog issue #105, maintainer decision). Fable 5 is the most expensive model in §3 — **$10/$50 per MTok, 2× Opus 5 and 3.3× Sonnet 5** — and Anthropic documents effort as *the* primary cost control for it: "Lower effort settings on Claude Fable 5 still perform well and often exceed `xhigh` performance on prior models." The step stops at `high` (the documented default) rather than `medium`/`low`, which the same guidance scopes to "routine work"; a merge gate is not routine. Continues the direction set on 2026-07-21, when the Reviewer went `max` → `xhigh` — same lever, cost motive instead of overthinking. **The originally proposed change was rejected on the maintainer's own rule:** moving the Reviewer to `claude-opus-4-8 @ xhigh` would have put it on the **Builder's own tier** (Opus 5), which is §2's hard requirement and §4's recorded *tier collapse* failure mode — whose stated trigger is verbatim "switched to the Builder's model/tier to save cost" — and would have pinned the last quality gate to a **Legacy** model migrated off two days earlier (§7, 2026-07-26). Decisive against it: **Opus 4.8 and Opus 5 are priced identically** ($5/$25), so the saving came entirely from leaving Fable 5, not from choosing 4.8 — the tier collapse would have bought nothing. Sonnet 5 @ `xhigh` was offered as the cheaper *and* §2-safe alternative (−70% vs −50%) and declined, to keep the last gate on the strongest model. **Recorded as unmeasured in both directions** (new §4 row): the saving is unquantified because effort scales thinking volume per task, and the rehearsal's 12/12 with 0 bounces says nothing about reviewer strength. Same commit: §3 row + §3 as-of, metadata As-of/expiry, scaffold-tree pin, snippet, agent frontmatter (scaffold + self-hosted copy), integrity pin. | Maintainer decision (Horace Hou), 2026-07-28. Pricing and effort guidance verified same day: [models overview](https://platform.claude.com/docs/en/about-claude/models/overview), [effort](https://platform.claude.com/docs/en/build-with-claude/effort). | Horace Hou (decision) / Claude Opus 5 (build) |
| 2026-07-27 | **First Level-1 rehearsal — the pattern ran itself, live** (catalog issue #75, maintainer request). 12 real tickets through the full pipeline in a real repository: architect → builder → fresh-context reviewer → deterministic deliver, real branches, real PRs merged through the forge, real tracker issues closed. Two isomorphic 6-ticket sets so concurrency was the only variable; pointed at a sacrificial branch, so **`main` was never written by an agent**. Result: **12/12 delivered, 0 bounces, 0 escalations**, 94 tests green. **Measured 104.3 min at concurrency 1 vs 47.2 min at concurrency 4 — 2.21×**, against a model prediction of 3.0× (§4). **The first run delivered 0/6** and is the whole justification for Level 1: `git status --porcelain` collapses an entirely-untracked directory to `?? docs/`, which `deliver-ticket.mjs`'s path-anchored allowlist could not match, so every ticket was refused as "dirty" — invisible to 634 Level-0 checks and to the field (adopted repos escape it because `adopt` gitignores `docs/plans/`), and the E2E case that claimed to cover it (`P13`) passed **vacuously** because its fixture commits the plan. Fixed with `-uall` plus a `P14` that reproduces the collapse and fails without the fix (#83). Also recorded: the agents diagnosed that defect to `deliver-ticket.mjs:282`, proposed both fixes, and **refused to apply either** as out-of-File-scope, escalating instead — the role boundary holding under real conditions. Two §4 rows added (wave-model over-prediction; session-bound runs). **Status stays `trialed`**: promotion to `adopted` is the maintainer's call, and these tickets were synthetic (toy pure functions in the catalog's own testbed), not a real project's work. | Maintainer request (Horace Hou), 2026-07-27; runs `wf_8898975a-19a` (c=1) and `wf_ed25a757-c8c` (c=4); tickets RA-1..RA-6 (issues #77–#82) and RB-1..RB-6 (issues #90–#95). | Horace Hou (request) / Claude Opus 5 (build + analysis) |
| 2026-07-27 | **`/start-all` schedules globally, and the DAG is live** (catalog issue #71, maintainer decision — supersedes the open question in #70): `start-all.js` no longer composes `run-milestone`. It is its own scheduler over the **flat** ticket DAG, gated only by `blocked_by`, so independent modules overlap instead of queueing — the 55% the previous entry could only price (11 rounds → 5 on the 19-ticket fixture) is now actually reachable. Second half: **the DAG is not fixed for the duration of a run.** Every `rescanEvery` settled tickets (default 3), and always once more before finishing, the scheduler re-reads `docs/prd` through a low-effort agent running the new deterministic `dag-scan.mjs`, then publishes any new tickets and regenerates `dag.html`. Workflow scripts cannot touch the filesystem (`require`/`node:` are banned and suite-integrity enforces it), so that agent hop is the only available mechanism — not a stylistic choice. The forced final reload is a **correctness** floor, not cadence: without it a ticket added late is never seen, and "pending with nothing to unblock it" cannot distinguish a real cycle from a blocker not yet written. Every merge rule is explicit and tested: a late dependency on an already-dispatched ticket is **not** applied retroactively but escalated (§4); a mid-run cycle fails its members loudly; a failed scan keeps the previous graph so a half-written ticket cannot kill work in flight; a ticket deleted while pending is dropped; `maxTickets`/`maxRescans` bound runaway growth. **`run-milestone.js` is deliberately untouched** — `/start-milestone` and the nightly sweep still use it — which duplicates the dispatch loop across two workflow files that cannot import each other; the accepted mitigation is suite-startall's **parity test**, running both schedulers over identical static input and asserting identical per-ticket outcomes, so drift fails the gate instead of the field. Knock-on fixed in the same commit: `dag-report.mjs` had labeled global scheduling "not executable yet" and framed the delta as loss — now false, so the modes are relabeled `/start-all` (default) vs `/start-milestone`, the headline recommendation follows the global schedule, and the per-module figure is reported separately. E2E: suite-startall rewritten to 60 checks (module-barrier-gone overlap proof, cross-module gating, cascade, supervised stop, mid-run add, cadence + `rescanEvery: 0`, late-edge escalation, mid-run cycle, failed scan, pending deletion, runaway guards, validation, and 3 parity scenarios); suite-dagreport's four now-false assertions inverted. | Maintainer decision (Horace Hou), 2026-07-27. | Horace Hou (decision) / Claude Opus 5 (build) |
| 2026-07-27 | **One whole-PRD flowchart; module barrier priced** (catalog issue #68, maintainer request): the per-module cards from #66 are replaced by a **single flowchart of every ticket**, colored by module, with cross-module dependency edges drawn (dashed) — they were invisible in the per-module view. The request came with a premise — scheduling all tickets together is more efficient — and measuring it **confirmed the premise**: on a 19-ticket / 5-module fixture at the recommended concurrency of 4, the runner takes **11 ticket-rounds vs 5 globally scheduled (55% lost)**, because `start-all` awaits each `run-milestone` and two modules with no dependency between them still run in sequence. Notably the recommended concurrency is **4 either way** — the win comes from deleting the barrier, not from more lanes. Since the runner *cannot* execute a global schedule today, the page renders **both** schedules over the same graph with the global one explicitly labeled not executable, so it prices the barrier instead of drawing a run that never happens; recorded as a new §4 pitfall, and the runner change is tracked separately rather than smuggled in here. Categorical color computed, not chosen: cards from any two modules can sit adjacent, so this is the all-pairs case — the reference palette fails it at 8 slots, and an exhaustive subset search found **4** to be the largest set clearing the CVD and normal-vision floors in both light and dark (slots 1/4/5/6). Modules past the 4th take a neutral and stay identifiable by a per-card module chip, the legend, and click-to-highlight — no generated 9th hue. The chip is also the secondary encoding that makes dark mode's worst pair (CVD ΔE 6.9, in the 6–8 band) legal at all, so it is mandatory rather than decorative; a table view covers the light-mode contrast relief rule. E2E: suite-dagreport reworked to 60 checks — both schedules asserted against hand-computed rounds, schedule validity (no ticket before its blocker; every ticket exactly once), one-flowchart/no-per-module-sections, fixed-order color slots incl. the neutral overflow, and the single-module case where the barrier provably costs nothing. Model/effort table unchanged. | Maintainer request (Horace Hou), 2026-07-27. | Horace Hou (request) / Claude Opus 5 (build) |
| 2026-07-27 | **Execution DAG visualized at Gate 1** (catalog issue #66, maintainer request): `/breakdown-prd` now writes `docs/prd/dag.html` — a self-contained interactive view of the execution plan. New `dag-report.mjs` derives, per module, the wave breakdown and the **max useful lanes** by replaying run-milestone's own greedy scheduler at every concurrency from 1 to the ticket count; the PRD-wide **recommended `concurrency`** is the max across modules (never the sum — `start-all` awaits each module, so lanes are intra-module only). Motivation: issue #60 shipped parallel lanes as an opt-in integer with **no guidance**, so the value was being guessed — too low silently serializes independent work, too high just idles lanes — and a module that can only ever use one lane (a single `blocked_by` chain) was invisible until it ran, when it is a *file-scope decomposition* defect that Gate 1 is the last cheap moment to fix. Parsing/topo/simulation extracted to a shared `dag-core.mjs` that `milestone-dag.mjs` now imports, so the runner's DAG and the human's view cannot disagree; `milestone-dag.mjs`'s stdout contract is byte-unchanged (suite-dag D1–D8 unchanged and green). Page is deliberately **deterministic** — no timestamp, no resolved core count (the `min(16, cores-2)` harness cap is shown as a formula, since the machine that runs it is unknown at breakdown time) — so it can be committed and diffed in the breakdown PR without churning. `/start-all` now tells the agent to read the recommendation instead of defaulting to 1 silently. E2E: new suite-dagreport (47 checks — hand-computed lane/wave fixtures incl. cross-module edges that must NOT gate lanes, fan-in width, single-ticket and fully-parallel degenerate cases, byte-identical re-run, no external resource or network reference in the page, and all four loud-failure paths). Model/effort table unchanged. | Maintainer request (Horace Hou), 2026-07-27. | Horace Hou (request) / Claude Opus 5 (build) |
| 2026-07-26 | **Opus tier moved to Claude Opus 5; Architect trial ended** (maintainer decision): Builder `claude-opus-4-8 @ xhigh` → `claude-opus-5 @ xhigh`, and Architect `claude-fable-5 @ max` → `claude-opus-5 @ max`, which **closes the 2026-07-22 Fable-5-Architect trial (#48) after four days with no evidence recorded either way** — closed by the pattern's own successor-release expiry trigger (Opus 5 shipped; Opus 4.8 is now listed as legacy), not by a trial result. Effort levels unchanged for every role. Two grounding corrections made in the same pass rather than carried over silently: (1) the Builder row's SWE-bench Pro figure (Opus 4.8 **69.2** vs Sonnet 5 **63.2**) is **dropped** — it was a 4.8 number and no verified Opus 5 equivalent exists; (2) Anthropic's effort guidance **changed** with Opus 5 — 4.8 said "start at `xhigh` for coding and agentic use cases", Opus 5 says start at `high` with `xhigh` as the step-up "for demanding coding and agentic work" — so `xhigh` is now recorded as a `[team-policy]` step-up, not as the vendor's starting point. §4: the "Architect and Reviewer share a model" row is marked **resolved** (with the honest note that the exposure was never measured) and replaced by a new "Architect and Builder share a model" row; §2's hard requirement is unaffected — the Reviewer (Fable 5) is now a different model from *both* other roles. Same commit: §3 rows + §3 as-of, metadata As-of/expiry, scaffold-tree pins, snippet, agent frontmatter (scaffold + self-hosted copies), integrity + site E2E pins, catalog README index. **Open item — not done:** Anthropic advises a fresh effort sweep rather than reusing a prior model's setting; no sweep has been run, so both Opus 5 efforts are inherited, not measured. | Maintainer decision (Horace Hou), 2026-07-26. Model IDs, tier positioning, pricing, and effort guidance verified same day against live Anthropic docs — [models overview](https://platform.claude.com/docs/en/about-claude/models/overview) and [effort](https://platform.claude.com/docs/en/build-with-claude/effort). | Horace Hou (decision) / Claude Opus 5 (build) |
| 2026-07-20 | **Doc/site command coverage mechanized** (catalog issue #35, maintainer direction: `/start-all` had shipped but never appeared in the npm README or the catalog site, so users could not discover it — surfacing commands must be impossible to forget). The command frontmatter is the single source of truth: `build-site.mjs` now renders a Commands section per pattern from it (site complete by construction), README carries the full command list, and an E2E coverage gate asserts every `scaffold/.claude/commands/*.md` name appears in BOTH the generated site and README with a non-empty description (plus a non-vacuous sentinel guard). Adding a command without documenting it now fails the merge gate. No pattern-behavior change. | Maintainer direction (Horace Hou), 2026-07-20. | Horace Hou (direction) / Claude Opus 4.8 (build) |
| 2026-07-22 | **Architect model on trial** (catalog issue #48, maintainer decision): Architect `claude-opus-4-8 @ max` → `claude-fable-5 @ max`; Builder and Reviewer unchanged. Rationale: the Reviewer checks the implementation against the plan, not whether the plan was right, so an under-specified-but-consistent plan passes review silently — planning strength has value review cannot backstop. The maintainer's original proposal also moved the Reviewer to Opus 4.8; that was **rejected** as the §4 tier-collapse failure mode (Builder is Opus 4.8, and §2 makes different-tier-from-the-Builder a hard requirement), so only the Architect changed. Accepted cost recorded as a new §4 row: Architect and Reviewer now share a model. Explicitly a **trial** — no benchmark basis claimed (no verified Fable-5-vs-Opus-4.8 planning evidence exists on record); judge on real bounce rate and drift, revert or re-pin on evidence. Same commit: §3 row + §3 as-of, metadata As-of/expiry, scaffold-tree pin, snippet, integrity pins, §4, self-host re-sync. | Maintainer decision (Horace Hou), 2026-07-22. | Horace Hou (decision) / Claude Opus 4.8 (build) |
| 2026-07-21 | **Model/effort revised** (catalog issue #42, maintainer decision from field observation): Architect `claude-sonnet-5 @ xhigh` → `claude-opus-4-8 @ max` — plans were running too general and, being the fresh-context cold-start contract, under-specified plans drift downstream; the top model at `max` is invested in the highest-leverage stage (token budget not a constraint). Reviewer `claude-fable-5 @ max` → `@ xhigh` — Fable 5's capability margin (not effort headroom) is what catches edge cases at the gate, and `max` carries the documented overthinking/latency risk (§4). Builder and Triage unchanged; Reviewer stays a different model tier from the Builder. Same-commit: §3 table + as-of date, agent frontmatter, snippet, integrity pins. No benchmark claims invented — the Architect change is grounded in the `[internal]` observation + a `[team-policy]` choice. | Maintainer decision (Horace Hou), 2026-07-21. | Horace Hou (decision) / Claude Opus 4.8 (build) |
| 2026-07-20 | **Upstream issue escalation made opt-in** (catalog issue #40, maintainer decision): the CLAUDE.md snippet's "file pattern-level problems against the catalog" bullet — which hardcoded the maintainer's repo slug and a `gh issue create` instruction — is now wrapped in `<!-- upstream-escalation -->` markers and stripped by default. `adopt --upstream` opts in (bare = the catalog you adopted from; `--upstream <owner/repo>` retargets to a fork or internal catalog); the markers never leak into CLAUDE.md. Rationale: commercial and private adopters do not want an external repo slug or "file issues upstream" behavior written into their repo unasked. A neighboring line that named the product was also generalized to "the upstream pattern catalog". E2E: default adopt writes no catalog slug/marker/bullet; `--upstream` and `--upstream <repo>` include and retarget it. | Maintainer decision (Horace Hou), 2026-07-20. | Horace Hou (decision) / Claude Opus 4.8 (build) |
| 2026-07-20 | **adopt never guesses the platform** (catalog issue #38, maintainer decision, refining #34): the no-signal fallback no longer silently defaults to `gh`. Resolution order — `--platform`, origin host, repo-local signal (`.gitlab-ci.yml`/`.github/`); when none resolve, adopt prompts if interactive and otherwise exits without installing, asking for `--platform gh|glab`. This makes the agent-driven flow confirm the platform (adopt errors → agent asks the user → re-runs with `--platform`) before any infrastructure is built. E2E: no-signal non-TTY exits 1 and installs nothing; a repo-local signal still resolves without `--platform`. | Maintainer decision (Horace Hou), 2026-07-20. | Horace Hou (decision) / Claude Opus 4.8 (build) |
| 2026-07-23 | **Opt-in parallel lanes** (catalog issue #60, maintainer request): `run-milestone`/`start-all` gain a `concurrency` arg (default **1 = the sequential runner, byte-behaviour-unchanged**). `>1` (autonomous only; supervised forced to 1) runs independent tickets as DAG-scheduled parallel lanes from per-ticket `blockedBy` — builder + reviewer in isolated git worktrees (reviewer detached-checkouts the builder's commit), the architect's plan content passed to the isolated builder (worktrees can't see the gitignored plan), deliver serialized via a mutex (merges never overlap), a failed ticket cascades to skip its dependents, and an unsatisfiable in-run dependency fails loudly (no hang). Design **empirically validated first** (a sandbox git experiment confirmed: worktree-committed branches persist in shared `.git`, a dependent worktree from updated main sees its blocker, detached-checkout works while the branch is busy elsewhere, overlapping-scope merges conflict → escalate); Workflow worktree/concurrency semantics doc-verified (`min(16, cores−2)` cap). E2E: new suite-parallel (8 scenarios: lane overlap, blocker-waits, cascade-skip, deliver-serialized, supervised-forces-1, isolation+plan-content, cycle-guard, concurrency-cap); suite-runner unchanged proves `concurrency=1` identical. Cost: `>1` multiplies concurrent token spend, so opt-in per run. | Maintainer request (Horace Hou), 2026-07-23; design validated by sandbox experiment. | Horace Hou (request) / Claude Opus 4.8 (build) |
| 2026-07-23 | **MR/PR body from the repo template + `docs/plans` gitignore** (catalog issue #58, field report from Ruihang2017/Groundwork): the deliver step hardcoded a 3-section MR body, so an adopted repo's installed template (Summary/Type/Changes/Design note/Pipeline evidence/**Constraint check**/Evidence/Related) never applied to pipeline MRs — ironic given `adopt` tells you to fill the Constraint-check section. Fix (two layers): **mechanical** — `deliver-ticket.mjs` gains `--body-file` and reads the repo's `.gitlab/merge_request_templates` / `.github/pull_request_template.md` as the body skeleton (with `Closes #N` ensured), hardcoded body only as last-resort fallback; **content** — run-milestone's deliver agent composes the body by filling the template's semantic sections from the ticket + diff + CLEAR verdict + the repo CLAUDE.md non-negotiables (Constraint check), writing it to `.claude/tmp/<id>-mrbody.md`. Also: `docs/plans/*.md` (the Architect's untracked HOW plan — needs to exist, not be committed, #53) is now git-ignored by `adopt` and excluded from deliver's clean-tree guard, so untracked plans stop blocking delivery (the reporter had to move them by hand). Known limit: the *static* template's Constraint-check placeholder for human web-created MRs still can't be auto-filled from an arbitrary CLAUDE.md (documented). E2E: suite-deliver P12 (body-file/template/hardcoded) + P13 (docs/plans clean-tree), suite-runner S1 (deliver composes body), suite-adopt (gitignore). Per-ticket stages + model/effort table unchanged. | Field report (Horace Hou via Groundwork), 2026-07-23. | Horace Hou (report) / Claude Opus 4.8 (fix) |
| 2026-07-23 | **`pushmr` delivery sub-mode — MR API not required** (catalog issue #56, field report from Ruihang2017/Groundwork): a GitLab org whose token has the Issues API but a **403 MR API**, with a require-MR protected default branch, could deliver with **neither** mode (pr needs the MR API; direct needs to push a protected `main`). New `pushmr` (GitLab-only, auto-detected by a cheap MR-API probe): opens the MR over SSH with `git push -o merge_request.create/target/title/description` (no MR API), scraping the MR URL from the push's `remote:` output; the description is single-line with `Closes #N` (git forbids newlines in push-option values — the report's multi-line body isn't possible), and the CLEAR verdict is posted as an **issue comment** via the working Issues API; it stops for a human web-merge and a resume run closes/verifies via the Issues API. E2E: suite-deliver P11 (open → simulated web-merge → resume) against a bare origin that advertises push options + a pre-receive hook emitting the MR URL; fake-glab gained a 403-MR-API switch + `issue note`. Per-ticket stages + model/effort table unchanged. | Field report (Horace Hou via Groundwork), 2026-07-23. | Horace Hou (report) / Claude Opus 4.8 (fix) |
| 2026-07-23 | **Ticket is the executable source of truth** (catalog issue #53, maintainer decision — encode rule **(c) only**): the ticket (Architect-authored via `/breakdown-prd`) IS the spec (WHAT); `docs/plans/<id>.md` is HOW only and must never carry spec the ticket lacks — the ticket wins on any disagreement. Spec changes flow **ticket → docs PR → merge → `publish-tickets.mjs --sync` (issue re-published from the ticket, #52 mechanism) → execute**; never patch spec into a plan, code, or a hand-edited issue body. Encoded in architect.md (no-restate rule + a HOW-not-spec plan banner), builder.md, reviewer.md (new spec-fidelity check), plan-ticket.md, verify-delivery.md (writeback via the ticket), the CLAUDE.md snippet, and the ticket-template banner. The plan stays mandatory (`planExists` DoD unchanged) — options (a) on-demand / (b) mark-plan-HOW were deferred; the maintainer chose (c)-only. | Maintainer decision (Horace Hou), 2026-07-23. | Horace Hou (decision) / Claude Opus 4.8 (write-up) |
| 2026-07-23 | **Dependency DAG rendered into issue bodies** (catalog issue #52, field report from Ruihang2017/Groundwork): `publish-tickets.mjs` stripped all frontmatter before using it as the issue body, so `blocked_by`/`blocks` never reached the board — the DAG was visible only to `milestone-dag.mjs`, not to a human picking work. Now the body carries a rendered line — **Blocked by: #N · Blocks: #M** — resolving ticket ids to real issue **numbers** via a global `[<id>]`→number map (existing issues + created-this-run); an unpublished dependency shows `` `id` (pending) `` and a later `--sync` backfills it. New `--sync` flag regenerates EXISTING issue bodies from their ticket (backfills historical issues, and is the mechanism for issue #53's "edit ticket → docs PR → re-publish → issue updated" flow). E2E: suite-publish P9 (deps rendered with resolved #numbers + pending), P10 (`--sync` regenerates an existing body); fakes gained `issue edit`/`issue update` + a body-capture log. | Field report (Horace Hou via Groundwork), 2026-07-23. | Horace Hou (report) / Claude Opus 4.8 (fix) |
| 2026-07-23 | **PR-mode delivery** (catalog issue #50, field report from Ruihang2017/Groundwork): the deterministic deliver step merged locally and opened no PR/MR — 19 delivered tickets produced 0 PRs / 0 remote branches, and the "CLEAR verdict attached to the MR/PR" DoD item was structurally unsatisfiable. `deliver-ticket.mjs` gained a PR path (default via auto-detect: origin remote + authenticated forge): push the branch, open a PR/MR with `Closes #N` + the plan, post the Reviewer's CLEAR verdict as a **comment** (the durable review trail), merge **through the forge** (`gh pr merge`/`glab mr merge` — respects branch protection; a pending/failing required check escalates instead of force-landing), fast-forward the local default, close + verify. `--no-merge` makes `supervised` mode open the PR/MR and stop for the human. Local `--no-ff` merge kept as the no-forge fallback (`--delivery pr\|direct\|auto`). run-milestone forwards the verdict to the deliver step (written to `.claude/tmp/<id>-verdict.md`, posted by the script — the agent never writes the tracker itself, so the `gh pr` surface stays off the agent's Bash allowlist, issue #30). `/verify-delivery` item 2 is now mechanically checkable. E2E: suite-deliver P-series (gh/glab PR happy path, idempotent re-run, supervised `--no-merge`, blocked-merge escalation, explicit-`pr`-no-origin refusal) + updated suite-runner S1/S6; the fakes gained `pr`/`mr` verbs that perform a real merge into the bare origin. Per-ticket stages and the model/effort table unchanged. | Field report (Horace Hou via Groundwork), 2026-07-23; fix direction proposed in the issue. | Horace Hou (report) / Claude Opus 4.8 (fix) |
| 2026-07-20 | **Platform compatibility hardened** (catalog issue #34, maintainer question): `adopt.mjs` detection now uses multiple deterministic signals — origin host `gitlab`/`github` substring, then a repo-local signal (`.gitlab-ci.yml` → glab, existing `.github/` → gh), then default gh with a loud ambiguity note — so a self-hosted GitLab on a custom domain is no longer misread as GitHub. The resolved platform is recorded as a `Tracker:` line in the adopted CLAUDE.md, and `/start-milestone` · `/start-all` · `/nightly-issues` now read `platform` from that line instead of guessing (parallel to Operating mode / testCmd). E2E: self-hosted-GitLab detection, `Tracker:` line matches platform, ambiguity note. Runtime scripts' own `--platform`/autodetect unchanged. | Maintainer question (Horace Hou), 2026-07-20. | Horace Hou (question) / Claude Opus 4.8 (fix) |
| 2026-07-19 | **Field bug #4 fixed** (catalog issue #31, field report from Ruihang2017/Groundwork2): `milestone-dag.mjs` emitted each module's tickets in ID order instead of topologically by intra-module `blocked_by`, so a module whose declared order deviates from ID order dispatched a ticket before its blocker and failed at the Builder's precondition gate. Fix: a shared Kahn `topoSort` now orders both modules and, within each module, tickets by intra-module `blocked_by` (cross-module edges stay module-level; ID order as tiebreak; intra-module cycle fails loudly). E2E +7 (suite-dag D6 the 0502/0503 inversion, D7 intra-module cycle, D8 ID-order tiebreak). run-milestone dispatch loop unchanged. | Field report (Horace Hou via Groundwork2), 2026-07-18. | Horace Hou (report) / Claude Opus 4.8 (fix) |
| 2026-07-19 | **Pipeline tool surface explicitly pre-allowed** (catalog issue #30, maintainer direction): scaffold `settings.json` now enumerates allow rules for everything the stages legitimately run — deterministic scripts, git branch/commit/push/read set, `npm test`-family, `gh`/`glab` issue list/view/comment/edit|update|note/close/create — so a fully autonomous `/start-all` → deliver → nightly cycle triggers zero interactive approvals. Deliberately not pre-allowed (role discipline stays mechanical): `git merge`/`rebase`/`reset`/`clean`, `gh pr *`, and bare `git:*`/`gh:*`/`glab:*` wildcards — E2E asserts both directions so drift fails the gate. Risk accepted and recorded: `git push:*` includes force variants; `gh issue create:*` is broader than the publish-tickets discipline (nightly report issue needs it); prefix-deny of flags is structurally incomplete so not attempted. | Maintainer direction (Horace Hou), 2026-07-19. | Horace Hou (direction) / Claude Fable 5 (build) |
| 2026-07-19 | **Delivery made deterministic** (catalog issue #26, field report: harness safety classifiers blocked the agent-run deliver stage 3/3 after journaled CLEARs): new `.claude/scripts/deliver-ticket.mjs` — clean-tree guard, `--no-ff` merge (idempotent on re-runs, conflicts abort clean), push, tracker close with post-close verification (`--issue` or `[<id>]` title-prefix lookup), deterministic DoD, `DELIVER-SUMMARY-JSON` contract; run-milestone's deliver agent reduced to executing that one command and relaying its summary; `settings.json` gained explicit allow rules for the three deterministic scripts. E2E: new suite-deliver (12 scenarios on real temp git repos + fake CLIs). The tracker close only happens after merge AND push actually landed — a closed issue is what resume filtering treats as delivered, so closing on a failed delivery would silently drop the ticket. One DoD item relocated: the post-merge test run executes deterministically when `testCmd` is supplied (workflow arg, forwarded as `--test-cmd`; `/start-all` passes it through) and otherwise stays with `/verify-delivery` — builder and reviewer both already ran tests on the branch. Per-ticket stages before deliver, and the model/effort table, unchanged. | Field report (Horace Hou via Groundwork), 2026-07-18; fix direction proposed in the issue itself. | Horace Hou (report) / Claude Fable 5 (fix) |
| 2026-07-19 | **Field bug #3 fixed** (catalog issue #27, reported from Ruihang2017/Groundwork2): `/start-all` step 3 never stated that `modules[].tickets` elements must be `{id, path, issue}` objects joined from two sources (DAG plan order + per-module `PUBLISH-SUMMARY-JSON`) — the DAG JSON alone emits bare id strings, which run-milestone's validator rejects. Command doc now names the exact shape and the join; `/start-milestone` already documented it. (The issue's other defect — stringified Workflow `args` — was already fixed under #23 / v0.2.1.) | Field report (Horace Hou via Groundwork2), 2026-07-18. | Horace Hou (report) / Claude Fable 5 (fix) |
| 2026-07-18 | **Field bug #2 fixed** (catalog issue #23, same reporter channel): (a) #21's install-time LF fix did not survive target-side git checkouts — `adopt.mjs` now installs/appends a target-repo `.gitattributes` pinning `.claude/workflows/*.js` + `.claude/scripts/*.mjs` to `eol=lf` (marker-checked, append-once); (b) Workflow `args` can arrive JSON-stringified — all three workflow scripts gained the `typeof args === 'string' ? JSON.parse(args) : (args \|\| {})` shim. Both recorded as §4 pitfalls; E2E extended (stringified-args runner + start-all scenarios; `.gitattributes` install/append/idempotency). Per-ticket stages and model/effort table unchanged. | Field report (Horace Hou via Groundwork), 2026-07-18. | Horace Hou (report) / Claude Fable 5 (fix) |
| 2026-07-17 | **Catalog site**: claymorphic GitHub Pages page generated from pattern data (`scripts/build-site.mjs` — parses pattern README metadata/§1/§3 + package.json; live npm-version fetch with build-time fallback; deployed via `gh-pages` branch). Style spec + generated-not-handwritten rationale in catalog issue #17. | Maintainer direction (Horace Hou) with reference designs, 2026-07-17. | Horace Hou (direction) / Claude Fable 5 (build) |
| 2026-07-17 | **Released**: catalog published to public npm as `agent-templates` under MIT (maintainer decisions: MIT + personal ownership + publish, 2026-07-17 — catalog issue #15). README carries the scaffold-output attribution carve-out. Adoption command is now `npx agent-templates@latest adopt …`. | Maintainer decision (Horace Hou), 2026-07-17. | Horace Hou (decision) / Claude Fable 5 (release) |
| 2026-07-17 | **Registry decision — public npm over GitHub Packages** (repo is public; goal is public sharing): GH Packages requires a token even to install public packages (`[official]` GitHub docs, verified live 2026-07-17) and is scope-only; npm allows anonymous installs and the bare name `agent-templates` was unclaimed (registry 404, same day). Publish itself is gated on maintainer decisions — license (repo has none), npm account/name — tracked in catalog issue #13; `package.json` carries the publish-prep (`files`, `repository`) with `private: true` until then. | Maintainer direction (Horace Hou) + live verification, 2026-07-17. | Horace Hou (direction) / Claude Fable 5 (write-up) |
| 2026-07-17 | **Distribution**: the catalog is npx-runnable (`npx github:Ruihang2017/agent-templates adopt <pattern> <dir>` — root `package.json` bin → `scripts/cli.mjs` dispatcher; `private: true` blocks accidental registry publish, git-specifier installs unaffected). README gained the five-step quickstart; ADOPTING.md gained the team-distribution tiers (npx-from-git → GitHub Packages → public npm) and the org-account question (open, issue #11). | Maintainer direction (Horace Hou), 2026-07-17. | Horace Hou (direction) / Claude Fable 5 (build) |
| 2026-07-17 | **Adoption path** (maintainer request: "a folder with only a PRD.md"): one-command installer `scripts/adopt.mjs` (idempotent; scaffold + universal templates + docs skeleton + CLAUDE.md seed/append; E2E-tested incl. re-run and platform variants), new `/breakdown-prd` command closing the flow gap between a bare master PRD and ticket'd modules (Architect charter extended to planning artifacts: plans + decomposition outputs, still never production code), and the catalog-level `ADOPTING.md` walkthrough (new + existing projects). Catalog issue #9. | Maintainer direction (Horace Hou), 2026-07-17. | Horace Hou (direction) / Claude Fable 5 (build) |
| 2026-07-17 | Templates made **universal** (maintainer direction): source of truth relocated from this scaffold to the catalog root — `templates/ticket.template.md` upgraded to the field-proven PIL-15 / FND-9 standard (traceability header with ADR/PR chain + per-ticket agent-assignment basis, `lane`/`blocked_by`/`blocks` frontmatter DAG, per-item Non-goal owners, file-scope does-not-touch + serial-safety analysis, code-level deliverables, project-defined acceptance-tag vocabulary, enumerated feedback-obligation escalation paths). Added `templates/tracker/`: PR/MR templates (pipeline-evidence + constraint-check sections, after MeritAI's) and a `decision-record` issue template formalizing catalog issues #1–#4's genre. The catalog itself uses the same templates (`.github/` byte-synced, E2E-enforced). Catalog issue #7. | Maintainer direction (Horace Hou), 2026-07-17, citing realtime-pilot PIL-15 and MeritAI FND-9 + PULL_REQUEST_TEMPLATE.md (both read-only references). | Horace Hou (direction) / Claude Fable 5 (write-up) |
| 2026-07-17 | Documented the **upstream feedback channel** (target repos file pattern-level issues against the catalog, which self-hosts the nightly sweep to triage them). The catalog repo also adopted issue/PR decision-record discipline — see catalog issues #1–#5. | Maintainer direction (Horace Hou), 2026-07-17. | Horace Hou (direction) / Claude Fable 5 (write-up) |
| 2026-07-17 | Added the unattended **nightly issue sweep** (triage agent + `nightly-issues` workflow + `/nightly-issues` headless entry + native tracker issue templates so hand-written issues keep the pipeline format), the **testbed** (Level-0 deterministic E2E — 122 checks over guard/publisher/runner — plus the Level-1 live-rehearsal app; Level-0 green is now the merge gate for scaffold changes), `GH_BIN`/`GLAB_BIN` test-double overrides (fx `GLAB_BIN` precedent), and the **testing policy**: agents own unit/integration/E2E; the human's only test duty is the Gate 2 smoke test after the PRD completes. Headless/permission/scheduling facts verified against live docs (headless.md, permission-modes.md, permissions.md, scheduled-tasks.md, routines.md — record in `scaffold/INSTALL.md`). | Maintainer direction (Horace Hou), 2026-07-17; verified by testbed E2E 122/122 green. | Horace Hou (direction) / Claude Fable 5 (build) |
| 2026-07-17 | Pre-merge adversarial review (three parallel reviewers; script findings reproduced with fixtures and fake gh/glab CLIs) — all findings fixed. Runner: a reviewer infrastructure failure no longer consumes bounce budget or dispatches phantom fixes (one retry → escalate `reviewer-failed`); `delivered` now requires merged ∧ issueClosed ∧ dodPassed; plan paths computed in code, agent-returned values verified; branch mismatch = builder failure; empty-findings BOUNCE escalates; config strictly validated; `supervised` mode stops the run after each CLEAR for dependency safety, resumable because closed issues are filtered on re-run. Publisher: existence check now fetches the issue list once per run and matches the `[<id>]` prefix exactly (fixes wrong-issue-number matching and per-ticket search rate/consistency risks); create failures keep the machine-readable summary and exit 1; within-run duplicate-id dedupe; BOM and quoted-YAML titles handled; invalid tickets included in the summary. DoD gains an explicit "tests green" item (verify-delivery + deliver prompt). Status corrected `adopted` → `trialed` per the promotion rule — this pattern has not yet run a real ticket (fx-eye-tracking ran the sibling harness, not this pipeline). | Internal adversarial review, 2026-07-17. | Claude Fable 5 (fixes); status correction flagged for maintainer confirmation |
