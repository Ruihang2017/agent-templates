// Tiny pure-function library — the surface the testbed tickets extend.
// SMK-01 adds subtract(); SMK-02 adds divide() with a zero guard.

export function add(a, b) {
  return a + b
}

export function multiply(a, b) {
  return a * b
}
